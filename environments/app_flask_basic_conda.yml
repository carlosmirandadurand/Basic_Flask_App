# Conda environment for Data Science in Azure (windows local machine)
# Platform: Windows / 64 bits
# First Time Use:
#   $ conda env create --file ./environments/app_flask_basic_conda.lock.yml
# Use:
#   $ conda activate app_flask_basic_conda
# Creation for the record in Windows:
#   $ conda info --envs
#   $ conda env remove -n app_flask_basic_conda
#   $ conda env create --file ./environments/app_flask_basic_conda.yml 
#   $ conda activate app_flask_basic_conda
#   $ conda env export > ./environments/app_flask_basic_conda.lock.yml
name: app_flask_basic_conda
channels:
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - pip
  - pandas
  - numpy
  - matplotlib
  - scikit-learn
  - pyodbc
  - snowflake-connector-python
  - flask
  - flask-sqlalchemy
  - flask-login
  - pip:
    - datetime
    - python-dotenv
    - pathlib
    - argparse
    - requests
    - catboost 
    - azure-ai-ml
    - azure-keyvault-secrets
    - azure-identity
    - azure-devops
    - mlflow
    - ipykernel
    - yfinance
    - openai
    - nltk







